//export sert a etre visible dans les autres fichiers
export class Produit{

    //declaration des attributs(objet metier) dans les parenthéses 
    constructor(public id: number, public nom:string, public prix:number){
    }
}